---
name: VSCode
description: Launches Visual Studio code in a project directory.
version: 1.0.0
configs:
  - platform: windows
    app:
      name: code
      command: code.cmd
  - platform: linux
    app:
      name: code
      command: code
