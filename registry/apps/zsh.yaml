# yaml-language-server: $schema=https://schemas.sierrasoftworks.com/git-tool/v1/template.schema.json
name: zsh
description: Launches the zsh shell within a project directory.
version: 1.0.0
configs:
  - platform: any
    app:
      name: zsh
      command: zsh
